import { TicketsFilterPipe } from './tickets-filter.pipe';

describe('TicketsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
